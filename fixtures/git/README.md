# Local Git integration fixture

The extension tests create disposable repositories and a bare local remote at
runtime. No repository bytes, credentials, network access, or generated Git
metadata belong in this directory.
